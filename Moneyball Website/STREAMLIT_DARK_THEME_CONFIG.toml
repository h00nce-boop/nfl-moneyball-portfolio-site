# Optional: copy this into your Streamlit app repo as .streamlit/config.toml
# This makes the embedded dashboard match the dark portfolio site more consistently.
[theme]
base = "dark"
primaryColor = "#c8f05a"
backgroundColor = "#0a0c0f"
secondaryBackgroundColor = "#111318"
textColor = "#e8edf5"
font = "sans serif"
